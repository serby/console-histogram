{
  "name": "console-histogram",
  "description": "Output a histogram on the console",
  "version": "0.0.0",
  "dependencies": {
    "lodash.assign": "^4.2.0",
    "sprintf": "^0.1.5"
  },
  "scripts": {
    "lint": "eslint -f unix .",
    "inspect": "jsinspect",
    "pretest": "npm run lint && npm run inspect",
    "test": "istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "(istanbul check-coverage --statements 90 --branches 90 --functions 100 --lines 90 && rm -rf coverage) || echo Look at 'coverage/lcov-report/index.html' to find out more"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "author": "Paul Serby <paul@serby.net>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.0.0",
    "eslint-config-clock": "^1.0.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-standard": "^4.0.0",
    "istanbul": "^0.4.5",
    "jsinspect": "^0.12.7",
    "mocha": "^6.1.4"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:serby/console-histogram.git"
  }
}
